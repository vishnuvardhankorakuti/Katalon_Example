name: Katalon Test Execution

on:
  push:
    branches:
      - master
  schedule:
    - cron: '0 12 * * *'   # Daily at 5:30 PM IST
  workflow_dispatch:

jobs:
  katalon-tests:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Clear old Katalon Reports
        run: |
          echo "Removing old Reports folder if it exists"
          rm -rf Reports
          mkdir -p Report

      - name: Run Katalon Tests using Docker
        env:
          KATALON_API_KEY: ${{ secrets.KATALON_API_KEY }}
        run: |
          docker run --rm \
            -v "$GITHUB_WORKSPACE:/katalon/project" \
            katalonstudio/katalon:10.0.0 \
            katalonc.sh \
            -noSplash \
            -runMode=console \
            -projectPath=/katalon/project \
            -testSuitePath="Test Suites/TS1" \
            -executionProfile="default" \
            -browserType="Chrome" \
            -apiKey="$KATALON_API_KEY"

      - name: Upload Katalon Reports
        uses: actions/upload-artifact@v4
        with:
          name: katalon-test-reports
          path: Reports/
          
      
  
      

      - name: Send Email with Test Report
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: ${{ secrets.SMTP_SERVER }}
          server_port: ${{ secrets.SMTP_PORT }}
          username: ${{ secrets.SMTP_USERNAME }}
          password: ${{ secrets.SMTP_PASSWORD }}
          subject: "Katalon Test Execution Report"
          body: |
            Hi,

            The Katalon test execution has completed.
            Please find the attached report.

            Regards,
            GitHub Actions
          to: ${{ secrets.MAIL_TO }}
          from: GitHub Actions <${{ secrets.SMTP_USERNAME }}>
          attachments: Reports/**/*.html